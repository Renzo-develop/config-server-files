application:
  consume:
    client:
      url: http://localhost:8080/client
spring:
  kafka:
    consumer:
      bootstrap-servers: localhost:9092
      group-id: consumer
    bootstrap-servers: localhost:9092
  application:
    name: ms-debitcard
  data:
    mongodb:
      database: msdebitcard
      port: '27017'
      host: localhost
server:
  port: '8081'
properties:
  config:
    server: 'true'
